deploy:production:
  stage: deploy
  image: alpine:3.20
  rules:
    - if: $CI_COMMIT_TAG
      when: manual
  environment:
    name: production
    url: https://example.com
  before_script:
    - apk add --no-cache openssh-client
  script:
    - ./devops/scripts/deploy.sh "$CI_COMMIT_TAG"
