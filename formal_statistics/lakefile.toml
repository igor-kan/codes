name = "formal_statistics"
version = "0.1.0"
defaultTargets = ["FormalStatistics"]

# Linear Statistical Models — Simple linear regression,
# ordinary least squares, inference, ANOVA, and correlation,
# formalized on top of Mathlib.
[[require]]
name = "mathlib"
scope = "leanprover-community"
version = "v4.16.0"

[[lean_lib]]
name = "FormalStatistics"
