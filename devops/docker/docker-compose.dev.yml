services:
  api:
    build:
      context: .
      target: build
    command: npm run dev
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      NODE_ENV: development
    ports: ["9229:9229"]

  mailhog:
    image: mailhog/mailhog
    ports: ["8025:8025", "1025:1025"]
